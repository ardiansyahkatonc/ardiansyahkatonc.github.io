---
// -----------------------------------------------------------------------------
// DEPRECATED shim — kept for backward compatibility.
// The canonical brand mark now lives at `src/components/common/Logo.astro`.
// Prefer importing from `common/Logo.astro` in new code.
// -----------------------------------------------------------------------------
import Logo from '../common/Logo.astro';

interface Props {
  size?: 'sm' | 'md' | 'lg';
  class?: string;
}
const { size = 'md', class: className = '' } = Astro.props;
---

<Logo size={size} class={className} />
